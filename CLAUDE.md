# CLAUDE.md

See [AGENTS.md](AGENTS.md) for full project context, stack, structure, and conventions.

## Claude-specific

- Always update `README.md` and `docker-compose.yml` when behavior changes
- No comments unless the WHY is non-obvious
- No custom CSS — Nuxt UI components only
